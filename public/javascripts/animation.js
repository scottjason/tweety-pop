// katy score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".katy-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// levine score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".levine-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// beyonce score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".beyonce-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// bieber score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".bieber-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// rihanna score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".rihanna-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// eminem score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".eminem-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// miley score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".miley-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// bruno score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".bruno-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// adele score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".adele-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// swift score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".swift-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// timberlake score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".timberlake-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

// lovato score loading
i = 0;
setInterval(function() {
    i = ++i % 4;
    $(".lovato-score-loading").html("waiting"+Array(i+1).join("."));
}, 500);

