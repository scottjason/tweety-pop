// katy score loading
a = 0;
setInterval(function() {
    a = ++a % 4;
    $(".katy-score-loading").html("waiting"+Array(a+1).join("."));
}, 500);

aa = 0;
setInterval(function() {
    aa = ++aa % 4;
    $(".katy-average-analyzing").html("analyzing"+Array(aa+1).join("."));
}, 500);

// levine score loading
// b = 0;
// setInterval(function() {
//     b = ++b % 4;
//     $(".levine-score-loading").html("waiting"+Array(b+1).join("."));
// }, 500);

// // beyonce score loading
// c = 0;
// setInterval(function() {
//     c = ++c % 4;
//     $(".beyonce-score-loading").html("waiting"+Array(c+1).join("."));
// }, 500);

// // bieber score loading
// d = 0;
// setInterval(function() {
//     d = ++d % 4;
//     $(".bieber-score-loading").html("waiting"+Array(d+1).join("."));
// }, 500);

// // rihanna score loading
// e = 0;
// setInterval(function() {
//     e = ++e % 4;
//     $(".rihanna-score-loading").html("waiting"+Array(e+1).join("."));
// }, 500);

// // eminem score loading
// f = 0;
// setInterval(function() {
//     f = ++f % 4;
//     $(".eminem-score-loading").html("waiting"+Array(f+1).join("."));
// }, 500);

// // miley score loading
// g = 0;
// setInterval(function() {
//     g = ++g % 4;
//     $(".miley-score-loading").html("waiting"+Array(g+1).join("."));
// }, 500);

// // bruno score loading
// h = 0;
// setInterval(function() {
//     h = ++h % 4;
//     $(".bruno-score-loading").html("waiting"+Array(h+1).join("."));
// }, 500);

// // adele score loading
// i = 0;
// setInterval(function() {
//     i = ++i % 4;
//     $(".adele-score-loading").html("waiting"+Array(i+1).join("."));
// }, 500);

// // swift score loading
// j = 0;
// setInterval(function() {
//     j = ++j % 4;
//     $(".swift-score-loading").html("waiting"+Array(j+1).join("."));
// }, 500);

// // timberlake score loading
// k = 0;
// setInterval(function() {
//     k = ++k % 4;
//     $(".timberlake-score-loading").html("waiting"+Array(k+1).join("."));
// }, 500);

// // lovato score loading
// l = 0;
// setInterval(function() {
//     l = ++l % 4;
//     $(".lovato-score-loading").html("waiting"+Array(l+1).join("."));
// }, 500);

